print(" hola buenas tarde soy tu asisten basico de calculo de areas de un triangulo")
base = float(input("ingrese la base del tringulo: "))
area = float(input("ingresa la altura del triangulo:"))
resultado = (base * area) / 2
print("el resultado del triangulo es:", resultado) # calculadora de areas de un triangulo